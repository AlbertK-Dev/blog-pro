import { createTheme } from "@mui/material";

const btnColorTheme = createTheme({
    palette: {
      white: {
        main: '#ffffffee',
        light: '#fff',
        dark: '#ffffffc5',
        contrastText: 'black',
      },
    },
  });

  export default btnColorTheme